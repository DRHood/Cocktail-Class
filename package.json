{
  "name": "project-template",
  "version": "0.5.0",
  "description": "# Setup",
  "main": "server.js",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.13.20"
  },
  "engines": {
    "node": "10.x"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "start": "node server.js",
    "postinstall": "cd client && npm i && npm run build"
  },
  "author": "DRHood",
  "license": "BSL, Commons Clause"
}
